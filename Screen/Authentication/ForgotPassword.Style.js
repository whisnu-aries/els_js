import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  inputContainer: {
    gap: 16,
  },
  title: {
    fontSize: 20,
    fontWeight: "600",
  },
});

export default styles;
